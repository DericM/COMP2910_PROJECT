/* NO LONGER NECESSARY

function GridSection(entity, point) {
		var entity = entity;
		var point = point;

		this.setEntity(newEntity) {
			entity = newEntity;
		};
		
		this.getEntity() {
			return entity;
		};
	};
*/