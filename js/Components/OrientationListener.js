function OrientationListener() {
    console.log("orientationlistener is commented out");
    /*
    window.addEventListener("orientationchange", , false);

    function orientationCheck() {
        if (screen.orientation.angle == 0) {
            CANVAS_MANAGER.setCanvasesVisibility(true);
            document.getElementById("container").removeChild(document.getElementById("orientlistener"));
        } else if (screen.orientation.angle == 90) {
            CANVAS_MANAGER.setCanvasesVisibility(false);
            var orientMsg = RESOURCES.getImage("orientlistener");
            orientMsg.style.position = "relative";
            orientMsg.style.left = "0px";
            orientMsg.style.top = "0px";
            document.getElementById("container").appendChild(RESOURCES.getImage("orientlistener"));
        }
    }*/
}